<header class="font-sans
               m-3
               p-2
               border-b-4
               border-blue-500">
  <div class="flex justify-between">
    <h1 class="text-2xl
               font-semibold
               tracking-wide">
        <%= t(:career_tracker) %>🕴️
    </h1>

    <% if authenticated? %>
      <div class="col-span-6 sm:flex sm:items-center sm:gap-4">
        <div class="inline">
          <%= link_to t(:sign_out),
              session_path,
              data: { 'turbo-method': :delete},
              class: "rounded-md px-3.5 py-1 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer"
          %>
        </div>
      </div>
    <% end %>
  </div>
</header>
