<% if next_steps_past_due? %>
  <div class="text-red-500">
    <%= t(:past_due_tasks) %>: <%= @next_steps_past_due %>
  </div>
<% end %>
<% if next_steps_due_today? %>
  <div class="text-orange-500">
    <%= t(:tasks_due_today) %>: <%= @next_steps_due_today %>
  </div>
<% end %>
