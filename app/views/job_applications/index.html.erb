<div class="my-2">
  <%= render partial: 'layouts/search_form',
              locals: {
                path: job_applications_path,
                label: t(:find_application),
                width: 'w-1/4'
              }
  %>
</div>

<%= render partial: 'job_applications/applications_table',
            locals: {
              applications: @job_applications,
              caption: t(:all_applications)
            } %>
